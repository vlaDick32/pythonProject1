class Kozel:
    def __init__(self, name="Kozel"):
        self.name = name
        self.satiety = 100
        self.sound = sound
 def sound(self):
        self.sound = zvuk()
     else:
        satiety == 0
      if:
          sound(10)
      else:
          satiety == 100
      if:
         sound(0)


    class Turist:
        def __init__(self, name="turist"):
            self.name = name
            self.food = (100)
        if:
            self.food <=0



